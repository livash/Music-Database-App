<h2>INFO about <%= @band.band_name %> </h2>

<% if @albums %>

<h3> Albums </h3>
<ol>
	<% @albums.each do |album| %>
	<li>
		<%= album.album_name %>
	</li>
	<% end %>
</ol>

<% end %>

<br>
<%= button_to "Delete", band_url(@band), :method => "delete" %>
<br>
<%= link_to "Add new band", new_band_url %>
<br>
<%= link_to "Add new album", new_album_url %>
<br>
<%= link_to "All Albums", albums_path %>
<br>
<%= link_to "All tracks", tracks_path %>