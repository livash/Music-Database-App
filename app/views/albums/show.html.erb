<h2>INFO for album <%= @album.album_name%></h2>

<% if @album.tracks %>

<h3> Tracks </h3>
<ol>
	<% @album.tracks.each do |track| %>
	<li>
		<%= track.track_name %>
	</li>
	<% end %>
</ol>
<% end %>
<br>
<%= button_to "Delete", album_url(@album), :method => "delete" %>
<br>
<%= link_to "Add new band", new_band_url %>
<br>
<%= link_to "Add new album", new_album_url %>
<br>
<%= link_to "Add new track", new_track_url %>
<br>
<%= link_to "All Albums", albums_path %>
<br>
<%= link_to "All tracks", tracks_path %>
